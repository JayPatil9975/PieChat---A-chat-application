/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn-icons-png.flaticon.com', 'upload.wikimedia.org','via.placeholder.com'], // Add domains here
  },
};

export default nextConfig;
